<Page
    x:Class="Gardenr.Views.Catalogus"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gardenr.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:Gardenr.ViewModels"
    xmlns:uc="using:Gardenr.UserClass"
  >
    <Page.DataContext>
        <vm:CatalogusVM></vm:CatalogusVM>
    </Page.DataContext>

    <Grid Background="#01FF70">

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
     
        <TextBlock Grid.Row="0"  FontSize="28" Margin="48 0 0 0" VerticalAlignment="Center">Catalogus</TextBlock>

        <ListView HorizontalContentAlignment="Stretch"  Name="ListItems" SelectedItem="{Binding SelectedPlant, Mode=TwoWay}"    ItemsSource="{Binding Planten, Mode=OneWay}" Grid.Row="1">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Background="Transparent" Padding="10 10 10 10">
                        
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding FotoUrl}"  Grid.Row="0" Grid.Column="0"  Grid.RowSpan="3" ></Image>
                        <TextBlock  Margin="10 0 0 0 " TextWrapping="Wrap" HorizontalAlignment="Stretch" Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Text="{Binding Omschrijving}"></TextBlock>
                        <TextBlock Margin="10 0 0 0 " TextWrapping="Wrap"  Grid.Row="0"  Grid.Column="1" Text="{Binding Naam}"></TextBlock>
                        <Button  Margin="10 0 0 0 " Content="Meer info" Grid.Row="1" Grid.Column="2" Command="{Binding GoToDetail}" >
                            <Button.DataContext>
                                <vm:CatalogusVM></vm:CatalogusVM>
                            </Button.DataContext>
                            
                        </Button>

                    </Grid>

                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

    </Grid>
</Page>
