<Page
    x:Class="Gardenr.Views.NotificatiesBewerken"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gardenr.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Gardenr.ViewModels"
    mc:Ignorable="d">
    <!--DataContext="{Binding NotificatiesBewerken, Mode=TwoWay, Source={StaticResource Locator}}">-->
    <Page.DataContext>
        <vm:NotificatiesBewerkenVM></vm:NotificatiesBewerkenVM>
    </Page.DataContext>
    
    <Page.Resources>
        <Style x:Key="headerS" TargetType="Rectangle">
            <Setter Property="Fill">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#0ba14b" Offset="0"/>
                        <GradientStop Color="#0ba14b" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="bodyS" TargetType="Rectangle">
            <Setter Property="Fill" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="notificationS" TargetType="Grid">
            <Setter Property="Background" Value="#abd3a8"/>
        </Style>
        <Style x:Key="TextS" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="Text2" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#659d46"/>
        </Style>
    </Page.Resources>
    <Grid Background="#01FF70">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="379*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.Row="0" Style="{StaticResource headerS}" Grid.ColumnSpan="4"></Rectangle>

        <TextBlock Grid.Row="0" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource TextS}" Grid.ColumnSpan="3" Grid.Column="1"  >Notificatie detail</TextBlock>



        <Rectangle Grid.Row="1" Grid.RowSpan="5" Style="{StaticResource bodyS}" Grid.ColumnSpan="4"/>

        <Button Content="Opslaan" Background="Transparent" Command="{Binding SaveSettings}" Grid.Row="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.ColumnSpan="4" FontSize="26" Foreground="DarkGreen"></Button>

        <TextBlock Grid.Column="1" Text="Datum" FontSize="20" Grid.Row="1" Style="{StaticResource Text2}" Margin="0,10" VerticalAlignment="Center" />

        <DatePicker  Date="{Binding Date, Mode=TwoWay}" VerticalAlignment="Stretch" Grid.Column="2"  FontSize="15" Grid.Row="1" HorizontalAlignment="Stretch" AllowDrop="True" Grid.ColumnSpan="1" Header="" Height="70" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <TextBlock  Grid.Column="1" Text="Notificatie type"  FontSize="20" Grid.Row="2" Style="{StaticResource Text2}" VerticalAlignment="Center"/>
        <ComboBox  SelectedItem="{Binding SelectedType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="2"  HorizontalAlignment="Stretch" VerticalAlignment="Center" ItemsSource="{Binding NotificatieTypes}" DisplayMemberPath="Naam" Height="32"
                   ></ComboBox>

        <TextBlock  Grid.Column="1" Text="Herhalen periode" FontSize="20" Grid.Row="3" Style="{StaticResource Text2}" VerticalAlignment="Center"></TextBlock>
        <Slider x:Name="slider" Grid.Column="2" Grid.Row="3" Value="{Binding NotAlarm.herhalen, Mode=TwoWay}" Minimum="0" Maximum="31" Margin="0,0,40,0" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="2" Text="{Binding ElementName=slider, Path=Value}" HorizontalAlignment="Right" Grid.Row="3" VerticalAlignment="Center" FontSize="20" Style="{StaticResource Text2}"></TextBlock>
 
        <TextBlock Grid.Column="1" Text="Mededeling" FontSize="20" Grid.Row="4" Style="{StaticResource Text2}" Margin="0,15,0,0"></TextBlock>
        <TextBox  Grid.Column="2" Text="{Binding BewNotificatie.Omschrijving, Mode=TwoWay}" FontSize="23" Grid.Row="4" Margin="0,15,0,5" TextWrapping="Wrap" PlaceholderText="Omshrijving"/>



    </Grid>
</Page>
