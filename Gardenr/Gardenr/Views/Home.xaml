<Page
    x:Class="Gardenr.Views.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gardenr.Views"
    xmlns:uc ="using:Gardenr.UserClass"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:Gardenr.ViewModels">
    <Page.DataContext>
        <vm:HomeVM></vm:HomeVM>
    </Page.DataContext>

    <Page.Resources>
        <Style x:Key="headerS" TargetType="Rectangle">
            <Setter Property="Fill">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#329465" Offset="0"/>
                        <GradientStop Color="#339461" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="bodyS" TargetType="Rectangle">
            <Setter Property="Fill" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="notificationS" TargetType="Grid">
            <Setter Property="Background" Value="#abd3a8"/>
        </Style>
        <Style x:Key="TextS" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style x:Key="Text2" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#659d46"/>
        </Style>
    </Page.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="41"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="41"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.Row="0" Style="{StaticResource headerS}"></Rectangle>
        <TextBlock Grid.Row="0" Text="" FontSize="28" Margin="57,4,0,4" VerticalAlignment="Center" Style="{StaticResource TextS}" >Home</TextBlock>
        <Rectangle Grid.Row="1" Grid.RowSpan="5" Style="{StaticResource bodyS}"/>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
           

            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,5,5" FontSize="13">weer door openweathermap.org</TextBlock>
            <Image Grid.Row="0" Source="{Binding Fotourl, Mode=OneWay}" Margin="0,5" Width="150" VerticalAlignment="Stretch" Stretch="Fill" ></Image>
            <TextBlock Grid.Row="1" Text="{Binding Weertext, Mode=OneWay}"  TextAlignment="Center" FontWeight="Bold"></TextBlock>
            <TextBlock Grid.Row="2" Text="{Binding Weerstatus, Mode=OneWay}"  TextAlignment="Center" ></TextBlock>
            <TextBlock Grid.Row="3" Text="{Binding Weerlocatie, Mode=OneWay}"  TextAlignment="Center" FontWeight="Bold"></TextBlock>
        </Grid>



        <TextBlock Margin="10,0,0,0" Text="Notificatie's" FontSize="25" Grid.Row="2" Style="{StaticResource Text2}"></TextBlock>
        <TextBlock Grid.Row="2" HorizontalAlignment="Right" Text="+" Style="{StaticResource Text2}" FontSize="40" Margin="0,-10,0,0" Tapped="TextBlock_Tapped"></TextBlock>
        <GridView  VerticalAlignment="Stretch" Name="ListItems"  SelectedItem="{Binding SelectedNotificatie, Mode=TwoWay}" ItemsSource="{Binding Notificaties, Mode=OneWay}" Grid.Row="3" Tapped="ListItems_Tapped" Margin="20,0,20,0">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="110">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="110"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding datum}" Grid.Row="1" TextAlignment="Center"/>
                        <TextBlock Text="{Binding Omschrijving}" Grid.Row="2" FontSize="10" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                        <TextBlock Text=">" Grid.Row="3" FontSize="20" TextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>


        </GridView>

        <TextBlock Margin="10,0,0,0" Text="Newsfeed" FontSize="25" Grid.Row="4" Style="{StaticResource Text2}">

        </TextBlock>
        <GridView VerticalAlignment="Stretch" Name="NiewsItems"  SelectedItem="{Binding SelectedNieuwsItem, Mode=TwoWay}" ItemsSource="{Binding NieuwsItems, Mode=OneWay}" Grid.Row="5"  Margin="10,0,0,0" Tapped="NiewsItems_Tapped">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Background="Transparent" Width="110">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>

                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Titel}" Grid.Row="1" TextAlignment="Center"/>
                        <TextBlock Text="{Binding Description}" Grid.Row="2" FontSize="10"/>

                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" x:Name="LocationDisabledMessage"  Margin="10,10,10,10" FontStyle="Italic" Visibility="Collapsed">
                <Run Text="Weer kan niet worden opgehaald zonder locatie toestemming. Gelieve te gaan naar " />
                <Hyperlink NavigateUri="ms-settings:privacy-location">
                    <Run Text="Settings" />
                </Hyperlink>
                <Run Text=" Om de permissies te wijzigen."/>
        </TextBlock>
    </Grid>
</Page>
